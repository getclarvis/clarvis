/** Private host/guest protocol revision baked into every compatible runtime artifact. */
export const RUNTIME_PROTOCOL_REVISION = "3";

/** OCI image label that carries the baked private runtime protocol revision. */
export const RUNTIME_PROTOCOL_LABEL = "io.clarvis.runtime.protocol";
