/** Re-exports the pure path helpers and `ownerFromWorkspace` from
 * `@clarvis/paths`, the shared filesystem leaf every writer of `.clarvis`
 * sources them from. */
export { ownerFromWorkspace } from "@clarvis/paths";
